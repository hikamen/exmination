"use strict";
//index.js

const app = getApp();
let util = require('../../utils/util');
let http = require('../../utils/http')

Page({
    data: {
    },
    onLoad: function () {
        console.log('home');
    }
});
